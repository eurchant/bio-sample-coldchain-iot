# Web 管理端 ZRQ 公网联调结果

> 联调日期：2026-07-25<br>
> 分支：`tkx`<br>
> 数据源：`http://47.103.152.175:8080/api/v1`

## 验证范围

- 已验证前端使用公网 API 地址，且公网接口向 `http://127.0.0.1:5173` 正确返回 CORS 许可。
- 已验证公开实时硬件快照可返回 `TASK-001`、`CLD-001` 和最新遥测。
- 已使用后端提供的管理员、发件人、接收人测试账号完成登录、任务列表、角色菜单与只读接口联调。
- 未创建或修改任务、交接、签收、告警、设备和文件；Token 仅保留在测试浏览器会话中并在测试后退出。

## 结果

| 角色 | 登录 | 任务列表 | 任务详情/遥测/交接/报告 | 设备 | 实时大屏 | Web 菜单 |
| --- | --- | --- | --- | --- | --- | --- |
| 管理员 | 通过 | 2 条任务 | 通过 | 通过 | 通过 | 任务、设备、大屏 |
| 发件人 | 通过 | 0 条任务 | `TASK-001` 返回 404 | 通过 | 403 | 任务、设备 |
| 接收人 | 通过 | 0 条任务 | `TASK-001` 返回 404 | 接口可访问，页面入口隐藏 | 403 | 仅任务 |

管理员大屏已从后端聚合接口加载实时数据；浏览器管理端控制台未发现错误。

## 角色界面结论

- 管理员可见任务总览、设备台账、实时大屏和任务新建/编辑入口。
- 发件人可见任务总览、设备台账和任务新建入口；尝试访问 `/screen` 会被前端路由返回任务总览并显示权限提示。
- 接收人仅可见任务总览，页面隐藏设备、大屏和新建任务入口。
- 任务详情中的交接、告警处置、签收等按钮仍由前端角色控制，并由后端接口再次校验。

## 待后端/组内协作

当前发件人和接收人均未被指派任何任务，因此无法在不修改真实数据的前提下验证其“发出 → 二维码交接 → 签收/拒收 → 追溯报告”完整流程。

请后端将 `TASK-001` 或专用测试任务分别指派给这两个测试账号，并提供可回收的测试任务标识；Web 端即可继续进行动作级联调。
